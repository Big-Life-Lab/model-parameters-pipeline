% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_parameters_pipeline_utils.R
\name{.rename_output_columns}
\alias{.rename_output_columns}
\title{Rename output columns to use a standard prefix/suffix scheme}
\usage{
.rename_output_columns(mod, prefix = "output", suffix = "_#")
}
\arguments{
\item{mod}{A pipeline model object. Must contain `$data` (a data frame) and
`$steps_info` (a list of step metadata), where the last element of
`steps_info` has an `$output_columns` field.}

\item{prefix}{Character string used as the base name for output columns.
Defaults to `"output"`.}

\item{suffix}{Character string appended to `prefix` (with `"#"` replaced by
an integer) when the bare prefix is already taken. Must contain `"#"`.
Defaults to `"_#"`, producing names like `"output"`, `"output_2"`,
`"output_3"`, etc.}
}
\value{
The modified pipeline model object with output columns renamed in
  both `$data` and the last element of `$steps_info$output_columns`.
}
\description{
Renames the output columns of the last step in a pipeline model object so
that they follow a consistent naming convention based on the given prefix and
suffix. Each output column is assigned a unique name via
[.get_unused_column()], avoiding collisions with existing columns in the
data.
}
\keyword{internal}
